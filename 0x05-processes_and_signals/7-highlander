#!/usr/bin/env bash
# This script loops indefinitely and "survives" SIGTERM signals

# Define the trap: when SIGTERM is received, print the message
trap 'echo "I am invincible!!!"' SIGTERM

# Infinite loop
while true
do
    echo "To infinity and beyond"
    sleep 2
done
